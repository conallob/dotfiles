eval "$(atuin init zsh)"

# Machine Specific zshrc
source $(chezmoi source-path)/zsh/zshrc.$(uname -s)

# Shell agnostic variables and aliases

source $(chezmoi source-path)/env
source $(chezmoi source-path)/alias
