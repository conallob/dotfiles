# ssh-agent setup

setenv SSH_AUTH_SOCK "$HOME/.ssh/agent-forwarded"

# zeus specific setup

screen -t zeus       		0 
screen -t dionysus     	1
screen -t hermes		   	2	
screen -t galvatron		 	3
screen -t galvatron		 	4
screen -t rodimus		    5
screen -t rehab3				6
