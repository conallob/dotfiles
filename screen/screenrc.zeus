# ssh-agent setup

setenv SSH_AUTH_SOCK "$HOME/.ssh/agent-forwarded"

# zeus specific setup

screen -t zeus       		0
screen -t dionysus     	1	# TODO(conall): ssh to dionysus once it exists
screen -t hermes		   	2	ssh hermes
screen -t galvatron		 	3	ssh galvatron
screen -t galvatron		 	4	ssh galvatron
screen -t rodimus		    5	ssh rodimus
screen -t rehab3				6	ssh rehab3
