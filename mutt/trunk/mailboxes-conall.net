# $Id$

#
# Personal Mail
#

# Default Mailbox

mailboxes = !

# Check if Ciara sent me anything

mailboxes = "=ciara"

# Check For Job Emails

mailboxes = "=jobs"

# Cron Jobs

mailboxes = "=cron"

# InfoCAD and clients

mailboxes = "=infocad"
mailboxes = "=designstrat"
mailboxes = "=odoh"

# ILUG, SAGE-IE, IE-NOG, BUGI and IIU

mailboxes = "=ilug"
mailboxes = "=sage-ie"
mailboxes = "=ienog"
mailboxes = "=bugi"
mailboxes = "=iiu"

# Fluxbox lists...

mailboxes = "=fluxbox-devel"
mailboxes = "=fluxbox-users"

# Debian Mailing Lists

mailboxes = "=debian-user"
mailboxes = "=debian-devel"
mailboxes = "=debian-mentors"
mailboxes = "=debian-debbugs"
mailboxes = "=debian-wnpp"
mailboxes = "=debian-security"
mailboxes = "=debian-laptop"
mailboxes = "=debian-accessibility"
mailboxes = "=debian-apache"
mailboxes = "=debian-firewall"
mailboxes = "=debian-ipv6"
mailboxes = "=debian-x"
mailboxes = "=debian-ssh"
mailboxes = "=debian-cd"
mailboxes = "=debian-doc"
mailboxes = "=debian-i18n"
mailboxes = "=debian-desktop"
mailboxes = "=debian-testing"


# Apple Lists...

mailboxes = "=apple-applescript-users"
mailboxes = "=apple-ipv6-dev"

# Ubuntu Mailing Lists...

mailboxes = "=ubuntu-devel"
mailboxes = "=ubuntu-users"

# Security Mailing Lists

mailboxes = "=securityfocus-bugtraq"

# Mozilla Bugzilla Stuff

mailboxes = "=mozilla"

# Postfix Mailing Lists

mailboxes = "=postfix-users"

# Linux Kernel

mailboxes = "=linux-kernel"

# NANOG

mailboxes = "=nanog"

# XEN

mailboxes = "=xen-users"

# Fink

mailboxes = "=fink-beginners"
mailboxes = "=fink-devel"
mailboxes = "=fink-dev"
mailboxes = "=fink-users"
mailboxes = "=fink-commits"
mailboxes = "=fink-core"
mailboxes = "=fink-i18n"
mailboxes = "=fink-tracker"

# US Cert

mailboxes = "=cert-tech-alerts"
mailboxes = "=cert-tech-bulletins"

# Subversion Stuff...

mailboxes = "=svn-users"
mailboxes = "=svn-dev"
mailboxes = "=svn-breakage"
mailboxes = "=svn-issues"
mailboxes = "=svn-commits"

# SVK stuff...

mailboxes = "=svn-dev"

# Torrents

mailboxes = "=torrents"

# 6BONE

mailboxes = "=6bone"

# Filtered spam

mailboxes = "=spam"
